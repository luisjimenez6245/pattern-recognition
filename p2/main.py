from views import MainView, ModelView, ClassView
import tkinter as tk

root = tk.Tk()
root.geometry("500x200")
window = MainView(master = root)
window.master.title("Clasificador Bayersiano")
window.mainloop()
window.mainloop()